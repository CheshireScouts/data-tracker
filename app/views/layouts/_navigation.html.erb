<span class="brand"><%= link_to "Data Tracker", root_path %> <%= "#{yield(:app_name)}" %></span>
<% if user_signed_in? %>
  <ul class="nav">
    <li><%= link_to 'Membership', memberships_path %></li>
  </ul>
<% end %>

<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <li>
    <%= link_to 'Login', new_user_session_path %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Edit account', edit_user_registration_path %>
    </li>
    <% if current_user.has_role? :admin %>
      <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin<b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><%= link_to 'Users', users_path %></li>
          <li><%= link_to 'Organisations', organisations_path %></li>
          <li><%= link_to 'Organisation Types', organisation_types_path %></li>
          <li><%= link_to 'Years', years_path %></li>
        </ul>
      </li>
    <% end %>
  <% end %>
</ul>
